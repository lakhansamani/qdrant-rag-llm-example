# Core RAG dependencies
qdrant-client[fastembed]>=1.9.0    # Qdrant vector DB + FastEmbed embeddings
gradio>=4.44.0                      # Web UI

# Fine-grained authorization (optional — only needed for the FGA demo:
# scripts/fga_seed.py, scripts/fga_demo.py, src/app.py --authorizer)
# Official Authorizer Python SDK. Not yet on PyPI; install from GitHub.
authorizer-py @ git+https://github.com/authorizerdev/authorizer-py.git  # Authorizer + OpenFGA

# Testing
pytest>=8.0.0                       # Test runner
pytest-cov>=5.0.0                   # Coverage reporting

# Development (optional)
ruff>=0.5.0                         # Fast Python linter
mypy>=1.10.0                        # Static type checker

# Note: Ollama is NOT a Python package.
# Install it separately from https://ollama.com/download
# Then pull a model: ollama pull llama3.2
